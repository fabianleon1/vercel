{
  "dependencies": {
    "express": "^4.18.1",
    "gh-pages": "^4.0.0",
    "morgan": "^1.10.0",
    "save-dev": "^0.0.1-security",
    "homepage": "https://{fabianleon1}.github.io"
  },
  "devDependencies": {
    "nodemon": "^2.0.19",
    "predeploy": "npm run build",
"deploy": "gh-pages -d build"
  }
}
